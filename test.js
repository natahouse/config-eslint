let x = 10
x += 10
const array = [1, 2, 3]

const array2 = [
  1,
  2,
  3,
]
const nata = 'house'

function name() {
  return ''
}

export default () => {
  const obj = {
    x,
    y: 20,
    z: 'teste',
    nata,
    name,
    array,
    array2,
  }

  return (obj)
}
